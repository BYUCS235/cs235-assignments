#pragma once

#include <vector>
using std::vector;

#include <algorithm>
using std::swap;

template <class T>
void sort(vector<T> &stuff)
{
    // After you finish HW3c, include your insertion-sort implementation here
    // to test its performance against the other sorts. 
}